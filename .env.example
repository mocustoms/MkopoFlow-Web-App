# Local development — copied to .env
VITE_API_URL=http://localhost:3000

# Railway (set on the web service; VITE_* vars are baked in at image build)
# VITE_API_URL=https://your-backend.up.railway.app
#
# If MkopoFlow-Backend is private, add a GitHub PAT with repo read access
# as a build-time variable so Docker can clone packages/shared:
# GITHUB_TOKEN=ghp_...
